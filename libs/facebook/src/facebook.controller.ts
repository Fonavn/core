import { Controller } from '@nestjs/common';

@Controller('facebook')
export class FacebookController {
  // webhook: not for now
  // peristent menu
  // setPageFeedWebhook: not for now
  // white listing website
}
